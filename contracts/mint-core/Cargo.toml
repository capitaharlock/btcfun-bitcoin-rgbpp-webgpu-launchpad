[package]
name = "mint-core"
version = "0.1.0"
edition = "2021"
description = "The standard tokenomics of PROTOCOL.md §4: reward, challenge and launch terms, free of any chain API so it can be tested on the host."

[dependencies]
sha2 = { workspace = true }

[dev-dependencies]
serde_json = "1"
